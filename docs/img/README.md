# Screenshots — placeholders to fill

The top-level `README.md` embeds five screenshots from this folder. They are **TODO placeholders**:
run `make demo`, open the app, and drop in the PNGs below (a clean ~1400px-wide capture each).

| file | page | what to capture |
|---|---|---|
| `markets.png` | Markets (`/`) | the 10-coin table with price / 24h % / market cap |
| `signals.png` | Signals (`/signals`) | a coin card: ML badge + confidence, prob bar, SHAP drivers, TA block |
| `analyst.png` | Analyst (`/analyst`) | the fused opinion: direction/conviction/agreement gauge + score breakdown + `healthSource` |
| `chat.png` | Researcher (`/chat`) | a cited answer with `[N]` chips + sources (needs Ollama up) |
| `performance.png` | Performance (`/performance`) | the equity curve + metrics panel after `make seed` |

Until these exist, the README image links render as placeholders — that is expected.
